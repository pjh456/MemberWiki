# 数据库文档总览

本目录沉淀数据库设计、关系说明和迁移规范，确保后端开发与联调阶段有统一的数据事实来源。

## 文件清单

- `schema.md`：核心表结构与字段约束
- `er-diagram.md`：实体关系说明（文字版）
- `migration.md`：迁移、发布与回滚规范

## 使用原则

1. 表结构变更必须先更新文档，再提交迁移脚本。
2. 破坏性变更必须在 `migration.md` 中提供回滚策略。
3. 上传文件只存路径与元数据，禁止存二进制文件本体。
